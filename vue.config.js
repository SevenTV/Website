// vue.config.js
/**
 * @typedef { import("@vue/cli-service").ProjectOptions } Options
 */

/** @type {Options} */
const options = {
  devServer: {
    host: "localhost",
    port: 3000,
  },
};

module.exports = options;
